<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.genesis.ai.app">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.example.app.permission.ACCESS_ORACLEDRIVE_SERVICE" /> <!-- ADDED -->

    <!-- Storage Permissions -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- For Android 10 and below -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- For Android 14+ Photo Picker -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Other Permissions -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission
        android:name="com.example.app.permission.ACCESS_ORACLEDRIVE_SERVICE"
        android:protectionLevel="signature" /> <!-- ADDED -->

    <application
        android:name=".GenesisApplication"
        android:allowBackup="true" <!-- ADDED -->
        android:dataExtractionRules="@xml/data_extraction_rules" <!-- ADDED -->
        android:fullBackupContent="@xml/full_backup_content" <!-- ADDED -->
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round" <!-- ADDED -->
        android:requestLegacyExternalStorage="true" <!-- KEPT EXISTING -->
        android:supportsRtl="true"
        android:theme="@style/Theme.GenesisAndroidApp" <!-- KEPT EXISTING THEME -->
        android:usesCleartextTraffic="true" <!-- ADDED -->
        tools:targetApi="29"> <!-- KEPT EXISTING targetApi -->

        <activity
            android:name=".data.MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Handle photo picker intents -->
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />
                <category android:name="android.intent.category.OPENABLE" />

                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".data.FileManagerActivity"
            android:configChanges="orientation|screenSize"
            android:exported="false"
            android:theme="@style/Theme.GenesisAndroidApp" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.GenesisAndroidApp" />

        <activity
            android:name=".ui.HaloViewActivity"
            android:theme="@style/AppTheme"
            android:windowAnimationStyle="@style/DisperseMaterialization" />
        <activity
            android:name=".ui.FileManagerActivity"
            android:theme="@style/AppTheme"
            android:windowAnimationStyle="@style/DisperseMaterialization" />
        <activity
            android:name=".ui.FilePickerActivity"
            android:theme="@style/AppTheme"
            android:windowAnimationStyle="@style/DisperseMaterialization" />
        <activity
            android:name=".ui.HelpActivity"
            android:theme="@style/AppTheme"
            android:windowAnimationStyle="@style/DisperseMaterialization" />
        <activity
            android:name=".ui.SettingsActivity"
            android:theme="@style/AppTheme"
            android:windowAnimationStyle="@style/DisperseMaterialization" />

        <service
            android:name=".service.GenesisAIService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".ipc.AuraDriveServiceImpl" <!-- ADDED SERVICE -->
            android:enabled="true"
            android:exported="true"
            android:permission="com.example.app.permission.ACCESS_ORACLEDRIVE_SERVICE">
            <intent-filter>
                <action android:name="com.example.app.ipc.IAuraDriveService" />
            </intent-filter>
        </service>

        <!-- Photo Picker for Android 14+ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>